{
  "license": "MIT",
  "scripts": {
    "watch": "parcel index.html --no-cache",
    "build": "parcel build index.html --public-url ./ --out-dir public --no-cache",
    "test": "xo && npm run build"
  },
  "dependencies": {
    "list-github-dir-content": "^1.1.1",
    "save-file": "^2.3.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.1",
    "xo": "*"
  },
  "xo": {
    "envs": [
      "browser"
    ]
  }
}
