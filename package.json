{
	"name": "download-directory",
	"private": true,
	"license": "MIT",
	"scripts": {
		"build": "parcel build",
		"fix": "xo --fix",
		"test": "xo && npm run build",
		"watch": "parcel serve"
	},
	"xo": {
		"envs": [
			"browser"
		],
		"rules": {
			"unicorn/better-regex": "off"
		}
	},
	"dependencies": {
		"list-github-dir-content": "^3.0.0",
		"p-map": "^5.5.0",
		"p-retry": "^5.1.1",
		"save-file": "^2.3.1"
	},
	"devDependencies": {
		"parcel": "^2.6.2",
		"xo": "^0.50.0"
	},
	"targets": {
		"default": {
			"source": "index.html",
			"distDir": "./public"
		}
	}
}
